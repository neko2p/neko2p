[package]
name = "ssh"
version = "0.1.0"
edition = "2024"
description = "wrapper for russh crate"

[dependencies]
anyhow = "1.0.95"
common = { path = "../../common" }
russh = "0.50.2"
tokio = { version = "1.43.0", features = ["net", "io-util"] }
